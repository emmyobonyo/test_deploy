<%= form_with(model: contactbook) do |form| %>
  <% if contactbook.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(contactbook.errors.count, "error") %> prohibited this contactbook from being saved:</h2>

      <ul>
        <% contactbook.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :email, style: "display: block" %>
    <%= form.text_field :email %>
  </div>

  <div>
    <%= form.label :number, style: "display: block" %>
    <%= form.text_field :number %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
